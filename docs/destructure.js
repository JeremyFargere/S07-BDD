const user = {
    name: 'lorenzo',
    age: 25,
};

// const name = user.name;
// const age = user.age;

const { name, age } = user;

console.log(name);

const express = {
    statics: function () {},
    Router: function () {},
};
